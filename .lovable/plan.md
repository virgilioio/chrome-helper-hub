

# Bump manifest version to 0.2.13

The manifest currently shows `0.2.12`, which was set before the extractor rewrite. The reactive multi-strategy extraction architecture is a significant change that should get its own version.

### Change
- `public/manifest.json` line 6: `"version": "0.2.12"` → `"version": "0.2.13"`

