

## Bump Manifest Version to 0.2.8

A one-line change in `public/manifest.json` to update the version from `0.2.7` to `0.2.8`, reflecting the API URL routing fix.

### Change

**`public/manifest.json`** -- Update version string from `"0.2.7"` to `"0.2.8"`

