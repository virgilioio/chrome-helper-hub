

# Bump Manifest Version to 0.2.14

Current version is `0.2.13`. Will bump to `0.2.14` to reflect the recent extraction quality fixes (name cleaning, pronoun filtering, location_state field).

### Change
**`public/manifest.json` line 6**: `"version": "0.2.13"` → `"version": "0.2.14"`

